#!/usr/bin/env node

'use strict';

const bootstrap = require('cordova-air-update-cli/dist/index');

process.title = 'angular-air-update';
process.on('unhandledRejection', function(r) { console.error(r); });

bootstrap.run();
